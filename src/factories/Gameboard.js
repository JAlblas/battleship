const Gameboard = () => {
    const missedAttacks = [];

    const receiveAttack = () => {
    }

    const isGameOver = () => {
        return false;
    }

    return {
        receiveAttack,
        isGameOver
    };
}

export default Gameboard;